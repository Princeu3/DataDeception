% Hand-written TikZ — methodology flowchart for DataDeception record construction.
\begin{tikzpicture}[
    node distance=5mm and 14mm,
    every node/.style={font=\footnotesize},
    box/.style={draw, rounded corners=2pt, align=center, minimum height=9mm, minimum width=32mm, inner sep=3pt, fill=white, font=\scriptsize},
    tier1/.style={box, fill=blue!12, draw=blue!60},
    tier2/.style={box, fill=green!12, draw=green!50!black},
    tier3/.style={box, fill=orange!15, draw=orange!70!black},
    tier4/.style={box, fill=gray!15, draw=gray!60},
    decision/.style={diamond, draw, aspect=2, inner sep=1pt, fill=yellow!25, draw=yellow!70!black, font=\scriptsize, align=center, minimum width=22mm},
    output/.style={box, fill=red!15, draw=red!70, minimum width=80mm, minimum height=12mm, font=\scriptsize, align=center},
    arr/.style={-{Latex[length=2mm]}, thick, gray!60!black},
    sectionlabel/.style={draw=none, font=\scriptsize\bfseries},
  ]
  % --- Section labels (top)
  \node[sectionlabel, text=blue!60!black] (claimlabel) {CLAIM};
  \node[sectionlabel, text=green!50!black, right=66mm of claimlabel] (reallabel) {REALITY};
  % --- claim side (left column)
  \node[tier4, below=2mm of claimlabel] (mfg)  {Manufacturer page\\(Tier 4)};
  \node[tier1, below=of mfg]            (reg)  {Regulatory filing\\(Tier 1: EPA / NHTSA)};
  % --- reality side (right column)
  \node[tier2, below=2mm of reallabel] (cr)    {Independent test\\(Tier 2: CR / MT / Edmunds)};
  \node[tier3, below=of cr]            (forum) {Owner forum\\(Tier 3, $N \geq 10$)};
  % --- evaluation
  \coordinate (mid) at ($(reg.south)!0.5!(forum.south)+(0,-12mm)$);
  \node[decision] (grade) at (mid) {Evidence\\grade};
  \node[output, below=10mm of grade] (rec) {\textbf{Deception record} (JSON)\\\scriptsize claim · reality · gap · severity · sources · caveats};
  % --- edges (claim side)
  \draw[arr] (mfg.south)   -- ++(0,-2mm) -| (grade.north);
  \draw[arr] (reg.east)    -- ++(8mm,0) |- (grade.west);
  % --- edges (reality side)
  \draw[arr] (cr.south)    -- ++(0,-2mm) -| (grade.north);
  \draw[arr] (forum.west)  -- ++(-8mm,0) |- (grade.east);
  % --- edge from decision to record
  \draw[arr] (grade.south) -- node[right, font=\scriptsize, gray!70!black, xshift=2mm] {A / B / C / D} (rec.north);
\end{tikzpicture}
